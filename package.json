{
  "name": "reemitter",
  "description": "JavaScript event emitter",
  "main": "emitter.js",
  "version": "1.1.4",
  "devDependencies": {
    "chai": "~1.8.1",
    "mocha": "~1.15.1"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha --no-colors --reporter spec",
    "preversion": "npm i && npm run test && npm run build",
    "build": "echo nothing for now",
    "postversion": "git push && git push --tags"
  }
}
